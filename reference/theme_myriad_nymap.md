# Myriad New York City map theme

Map theme suitable for NYC maps. You should \[import_myriad_semi\]()
first and also install the fonts on your system before trying to use
this theme.

## Usage

``` r
theme_myriad_nymap()
```

## Examples

``` r
if (FALSE) { # \dontrun{
} # }
```
